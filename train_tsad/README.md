# train_tsad

Training skeleton for zero-shot TSAD experiments.

## Quality Checks

Run the lightweight project self-check from the repository root:

```powershell
powershell -ExecutionPolicy Bypass -File .\train_tsad\scripts\check_quality.ps1
```

The current gate covers formatting, linting, type-checking, and smoke tests under `train_tsad/tests`.
